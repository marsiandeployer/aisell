---
status: done
depends_on: [1, 2, 3, 4]
wave: 4
skills: [code-writing]
verify: bash — `cd botplatform && npm test` exits 0
reviewers: [code-reviewer, test-reviewer]
teammate_name:
---

# Task 5: Update test files for SKILL.md

## Required Skills

Before starting, load:
- `/skill:code-writing` — [SKILL.md](/root/.claude/skills/code-writing/SKILL.md)

## Description

Tasks 1–4 renamed `CLAUDE.md.template` to `SKILL.md` in both products and updated `bot.ts` to reference the new path. The existing test suite (`test_folder_structure.js` and `test_claude_md_templates.js`) was written against `CLAUDE.md.template` and will fail after those changes.

This task updates both test files so the suite passes with 0 failures against the new SKILL.md structure.

The changes are non-trivial: beyond a path rename, the content model changed. SKILL.md no longer contains `{{PROJECT_IDEA}}`, `{USERID}`, or `Домен` (those were webchat-specific). It gained YAML frontmatter and a `## Live Examples` section with 7+ showcase links. Tests must reflect this new reality: old positive assertions removed or turned into negative assertions, new positive assertions added for frontmatter and showcase links.

The task also adds a test for `buildClaudeMdContent` behavior when the source file has no `{{PROJECT_IDEA}}` placeholder — confirming the function returns the original content unchanged, which is the intended behavior per Decision 2 in the tech-spec.

## What to do

1. Update `test_folder_structure.js` — Test 5 (product templates check):
   - Replace `CLAUDE.md.template` path lookup with `SKILL.md` for both `simple_dashboard` and `simple_site`
   - Change validation logic: instead of checking `{{PROJECT_IDEA}}` and `Безопасность` inside the file, check only that `SKILL.md` exists and is non-empty
   - Add negative check: assert that `CLAUDE.md.template` does NOT exist for each product (confirming rename happened)

2. Update `test_claude_md_templates.js` — comprehensive SKILL.md content tests:
   - Update the local `getClaudeMdTemplatePath()` helper function defined inside the test (lines 144–150): change path string from `CLAUDE.md.template` to `SKILL.md`
   - Update Test 1 (`Product templates exist`, lines 69–74): check `SKILL.md` exists instead of `CLAUDE.md.template`
   - Update Test 3 (`Required sections in templates`, lines 83–92): remove `{{PROJECT_IDEA}}`, `{USERID}`, `Домен` from `REQUIRED_SECTIONS`; keep `Безопасность`; change template path from `CLAUDE.md.template` to `SKILL.md`; add negative assertion that `{{PROJECT_IDEA}}` is absent
   - Update Test 4 (`Product-specific content in templates`, lines 94–103): change `CLAUDE.md.template` path to `SKILL.md` — this test checks `requiredInTemplate` markers (Chart.js, Tailwind, SPA, tt(, i18n, CAC for simple_dashboard; Tailwind, Hydra AI, data-i18n, Hero for simple_site). Without this path change the test silently skips via `if (!fs.existsSync) continue`, leaving all markers unverified.
   - Update Test 5 (`No internal content in client templates`, lines 105–114): change `CLAUDE.md.template` path to `SKILL.md` — this test checks `forbiddenInTemplate` markers (PromptBar, Showcases Architecture, Pre-commit Linters, config.yaml per showcase). Without this path change the test silently skips via `if (!fs.existsSync) continue`.
   - Update Test 7 (`Template substitution`, lines 126–139): remove substitution assertions for `PROJECT_IDEA` and `USERID`; change template path to `SKILL.md`; add new test for "no placeholder" path — read SKILL.md, confirm `{{PROJECT_IDEA}}` absent, run substitution (replace call), assert result equals original content and is non-empty
   - Update Test 8 (`getClaudeMdTemplatePath resolution`, lines 141–167): change both `CLAUDE.md.template` string checks to `SKILL.md` in the local helper and in the `includes()` assertions
   - Update Test 9 (`Developer vs client content separation`, lines 169–188): change `CLAUDE.md.template` path read to `SKILL.md`; flip the assertion at line 182 — SKILL.md should NOT have `{{PROJECT_IDEA}}` (both developer CLAUDE.md and SKILL.md are now placeholder-free)
   - Add new test block: YAML frontmatter checks for `simple_dashboard` — parse frontmatter block (between first `---` and second `---`), assert `name` is a non-empty string, `tags` is a non-empty array with at least 7 items, `description` is a non-empty string with length > 20
   - Add new test block: showcase links check — assert `simpledashboard.wpmix.net/showcases` appears at least 7 times in `simple_dashboard/SKILL.md`

3. Run `cd /root/aisell/botplatform && npm test` and confirm exit 0, 0 failed.

## TDD Anchor

This task updates existing tests — the process is inverted (tests exist, failing; fix them to pass). However, add the new assertions before verifying they pass:

- `botplatform/tests/test_claude_md_templates.js::test_skill_md_yaml_frontmatter` — new YAML frontmatter block: assert `name` non-empty, `tags.length >= 7`, `description.length > 20` for `simple_dashboard/SKILL.md`
- `botplatform/tests/test_claude_md_templates.js::test_skill_md_showcase_links` — showcase links block: assert `(content.match(/simpledashboard\.wpmix\.net\/showcases/g) || []).length >= 7` for `simple_dashboard/SKILL.md`
- `botplatform/tests/test_claude_md_templates.js::test_skill_md_no_placeholder` — "no placeholder" substitution: read SKILL.md, confirm no `{{PROJECT_IDEA}}`, call `.replace(/\{\{PROJECT_IDEA\}\}/g, 'test')`, assert result equals original and `result.length > 0`

Sequence: write new assertions → run test (new blocks pass if SKILL.md from Task 1 is correct) → fix path renames → run full suite → confirm 0 failed.

## Acceptance Criteria

- [ ] `test_folder_structure.js` Test 5 checks `SKILL.md` exists for both `simple_dashboard` and `simple_site`
- [ ] `test_folder_structure.js` Test 5 asserts `CLAUDE.md.template` does NOT exist for both products
- [ ] `test_claude_md_templates.js` Test 1 references `SKILL.md` (not `CLAUDE.md.template`)
- [ ] `test_claude_md_templates.js` `REQUIRED_SECTIONS` does not contain `{{PROJECT_IDEA}}`, `{USERID}`, or `Домен`
- [ ] `test_claude_md_templates.js` `REQUIRED_SECTIONS` still contains `Безопасность`
- [ ] `test_claude_md_templates.js` has negative assertion: `{{PROJECT_IDEA}}` is absent in SKILL.md
- [ ] `test_claude_md_templates.js` Test 4 reads `SKILL.md` (not `CLAUDE.md.template`) and actively verifies `requiredInTemplate` markers (Chart.js, Tailwind, SPA, tt(, i18n, CAC etc.) — does not silently skip
- [ ] `test_claude_md_templates.js` Test 5 reads `SKILL.md` (not `CLAUDE.md.template`) and actively verifies `forbiddenInTemplate` markers (PromptBar, Showcases Architecture etc.) — does not silently skip
- [ ] `test_claude_md_templates.js` has YAML frontmatter checks: `name` non-empty, `tags.length >= 7`, `description.length > 20`
- [ ] `test_claude_md_templates.js` has showcase links assertion: `simpledashboard.wpmix.net/showcases` appears >= 7 times
- [ ] `test_claude_md_templates.js` has "no placeholder" test for `buildClaudeMdContent`: substitution on SKILL.md returns original content unchanged
- [ ] Local `getClaudeMdTemplatePath()` helper in `test_claude_md_templates.js` references `SKILL.md`
- [ ] `cd /root/aisell/botplatform && npm test` exits 0 with 0 failed tests

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)
- [architecture.md](/root/aisell/products/simple_dashboard/.claude/skills/project-knowledge/references/architecture.md)
- [project.md](/root/aisell/products/simple_dashboard/.claude/skills/project-knowledge/references/project.md)
- [noxon-digital-factory.md](/root/.claude/skills/noxon-digital-factory.md)
- [test_folder_structure.js](/root/aisell/botplatform/tests/test_folder_structure.js)
- [test_claude_md_templates.js](/root/aisell/botplatform/tests/test_claude_md_templates.js)
- [products/simple_dashboard/SKILL.md](/root/aisell/products/simple_dashboard/SKILL.md) — created by Task 1; read to understand actual content before writing assertions

## Verification Steps

- Run `cd /root/aisell/botplatform && npm test` — must exit 0 with 0 failed tests
- Verify the output shows both test files ran without skips
- Confirm summary line reads `❌ Failed: 0` (or equivalent zero-failure output)

## Details

**Files:**

`botplatform/tests/test_folder_structure.js` — current state: Test 5 (lines 106–131) checks `CLAUDE.md.template` path and requires `{{PROJECT_IDEA}}` and `Безопасность` in the content. After Task 1, `CLAUDE.md.template` is deleted and `SKILL.md` exists in its place. The test will fail with "CLAUDE.md.template НЕ найден" for both products. Fix: change `path.join(productsDir, product, 'CLAUDE.md.template')` to `path.join(productsDir, product, 'SKILL.md')`, update validation condition to remove `{{PROJECT_IDEA}}` check, add separate `!fs.existsSync(templatePath_old)` assertion for each product.

`botplatform/tests/test_claude_md_templates.js` — current state: multiple places reference `CLAUDE.md.template`:
- Lines 60–65: `REQUIRED_SECTIONS` array includes `{{PROJECT_IDEA}}`, `{USERID}`, `Домен`, `Безопасность`
- Line 72–73: Test 1 loops and checks `CLAUDE.md.template` path
- Lines 86–87: Test 3 reads `CLAUDE.md.template` and asserts all `REQUIRED_SECTIONS` present
- Lines 97–98: Test 4 reads `CLAUDE.md.template` and checks `requiredInTemplate` markers per product (Chart.js, Tailwind, SPA, tt(, i18n, CAC for simple_dashboard; Tailwind, Hydra AI, data-i18n, Hero for simple_site). Has `if (!fs.existsSync(templatePath)) continue` guard — silently skips when file is missing.
- Lines 108–109: Test 5 reads `CLAUDE.md.template` and checks `forbiddenInTemplate` markers per product (PromptBar, Showcases Architecture, Pre-commit Linters, config.yaml per showcase). Has same silent-skip guard.
- Lines 129–131: Test 7 reads `CLAUDE.md.template`, substitutes `{{PROJECT_IDEA}}` and `{USERID}`, asserts substitution worked
- Lines 144–150: local `getClaudeMdTemplatePath()` helper references `CLAUDE.md.template` in path.join
- Lines 153–155, 157–159: Test 8 asserts path includes `simple_dashboard/CLAUDE.md.template` and `simple_site/CLAUDE.md.template`
- Lines 173–174: Test 9 reads both `CLAUDE.md` and `CLAUDE.md.template`
- Line 182: Test 9 asserts template has `{{PROJECT_IDEA}}`

**Dependencies:** Tasks 1, 2, 3, 4 must be complete before running `npm test`. Without `SKILL.md` existing at the product paths, even the updated tests will fail on file-not-found. Read `products/simple_dashboard/SKILL.md` before writing frontmatter assertions — the actual key names and tag count must match what Task 1 produced.

**Edge cases:**
- YAML frontmatter parse: use a simple string-split approach — find content between first `---` and second `---`, split by newlines, parse `key: value` and `key: [...]` patterns. Do not add a yaml parser package — keep it zero-dependency like the rest of the test file.
- Tags field in YAML may be formatted as inline array `[a, b, c]` or as a block list with `- item` per line. Handle both in the frontmatter parser, or read the actual Task 1 output to know which format was used and parse accordingly.
- `buildClaudeMdContent` "no placeholder" test: the goal is to confirm that when SKILL.md has no `{{PROJECT_IDEA}}`, calling `.replace(/\{\{PROJECT_IDEA\}\}/g, 'anything')` returns the file content unchanged. This mirrors the actual bot.ts behavior per Decision 2. The test does not need to call the real `buildClaudeMdContent` function — it can replicate the replace logic inline since the function is in compiled TypeScript and the test is plain Node.js.
- Test 9 (developer vs client separation): after Task 1, SKILL.md has no `{{PROJECT_IDEA}}` — so the assertion `template.includes('{{PROJECT_IDEA}}')` (line 182) must be flipped to `!template.includes('{{PROJECT_IDEA}}')`. The test description should be updated accordingly.

**Implementation hints:**
- Run `cd /root/aisell/botplatform && npm test` before making changes to see exactly which assertions fail and with what messages. This gives a precise list of what to fix.
- After changes, run `npm test` again to confirm no regressions in Test 2, Test 6, Test 10 which do not reference `CLAUDE.md.template` and should still pass unchanged.
- Keep the `assert()` helper and `log()` helper functions unchanged — only modify test logic inside the test blocks.
- The `PRODUCTS` object (lines 46–57) defines `requiredInTemplate` and `forbiddenInTemplate` markers. Tests 4 and 5 use these markers to verify SKILL.md content. After changing the path from `CLAUDE.md.template` to `SKILL.md`, these tests will run against the actual file — ensure SKILL.md from Task 1 actually contains all `requiredInTemplate` markers and none of the `forbiddenInTemplate` markers before confirming the task complete.
- The silent-skip guards (`if (!fs.existsSync(templatePath)) continue`) in Tests 4 and 5 are intentional for missing products but become a correctness hazard when the file was simply renamed. After the path fix, if any required marker is absent in SKILL.md the test will fail loudly — which is the desired behavior.

## Reviewers

- **code-reviewer** → `logs/working/task-5/code-reviewer-1.json`
- **test-reviewer** → `logs/working/task-5/test-reviewer-1.json`

## Post-completion

- [ ] Write a brief report to decisions.md (Summary: 1-3 sentences, review links, no findings dump)
- [ ] If deviated from spec — describe deviation and reason
- [ ] Update user-spec/tech-spec if anything changed
